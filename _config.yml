<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>{{ page.title | default: site.title }}</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 900px;
      margin: 50px auto;
      line-height: 1.6;
    }

    .nav {
      margin-bottom: 40px;
    }

    .nav a {
      text-decoration: none;
      font-weight: bold;
      color: #0366d6;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid #0366d6;
      margin-right: 10px;
    }

    .nav a:hover {
      background: #0366d6;
      color: white;
    }

    hr {
      margin: 30px 0;
    }

    .gallery {
      display: flex;
      overflow-x: auto;
      gap: 15px;
      margin: 15px 0;
    }

    .gallery img {
      width: 400px;
      border-radius: 8px;
    }
  </style>
</head>
<body>

<div class="nav">
  {% if page.lang == "ua" %}
    <!-- На українській сторінці показуємо тільки English -->
    <a href="resume_en.md">English 🇬🇧</a>
  {% elsif page.lang == "en" %}
    <!-- На англійській сторінці показуємо тільки Українську -->
    <a href="index.md">Українська 🇺🇦</a>
  {% endif %}
</div>

<!-- Контент сторінки -->
{{ content }}

</body>
</html>

